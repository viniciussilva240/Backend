import rl from 'readline-sync';


//Declaração da função:
let frase = rl.question('informe um texto para deixar Maiusculo: ')
console.log(caixaalta(frase));

function caixaalta(texto) {
    return texto.toUpperCase();
    
}

//expressão de fução
const cxAlta = function (texto){
    return texto.toUpperCase  
}

//função de seta
const tdMaiusculas = (texto) => texto.toUpperCase():
console.log(tdMaiusculas(frase));